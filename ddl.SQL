create database  Country;

use country;

CREATE TABLE STATE_INFO(
ID INT,
NAME VARCHAR(3)
);

INSERT INTO STATE_INFO(ID,NAME)VALUES(1,'KAR');

CREATE TABLE  CITY_INFO(
ID INT,
NAME VARCHAR(3)
);

INSERT INTO CITY_INFO(ID,NAME)VALUES(2,'BAN');

SELECT * FROM STATE_INFO;
SELECT * FROM CITY_INFO;

